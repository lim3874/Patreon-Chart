# v2.6.1 - Compact Period Chart Spacing

## Fixed

- Period chart bars now stay grouped near the center when only a few buckets are visible.
- Monthly charts with one or two bars no longer stretch those bars to the far left and far right edges.

## Verification

- `python -m py_compile patreon_member_gui.py export_patreon_members.py patreon_api.py`
- `python -m unittest discover -s tests`
- Local Tk smoke test confirming two monthly bars render near the center instead of the plot edges.
