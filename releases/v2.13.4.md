# v2.13.4 - Reduce Column Drag Lag

## Fixed

- Moved the Patreon API column drag preview back to a reused lightweight popup so the main table does not repaint under the moving preview.
- Kept the preview centered under the cursor while preserving the smoother popup-window movement path.
- Hid the popup during initial creation to avoid a first-frame flash before it is positioned.

## Verification

- `python -m py_compile patreon_member_gui.py export_patreon_members.py patreon_api.py discord_api.py`
- Tk smoke test for creating, centering, moving, hiding, and reusing the Patreon table drag preview popup.
