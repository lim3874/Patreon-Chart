# v2.12.1 - Patreon API Toolbar Cleanup

## Changed

- Removed the duplicate `Discord 봇 설정` button from the Patreon API toolbar.
- Discord bot credentials remain available from the main settings dialog.
- Kept `Discord 정보 채우기` in the Patreon API toolbar because it is an action for enriching existing Patreon rows, not a settings shortcut.

## Verification

- `python -m py_compile .\patreon_member_gui.py`
- `python -m unittest discover -s tests`
