# v2.7.2 - Windows Frame Dark Mode Fix

## Fixed

- The dark title bar fix now targets the real Windows decorated frame handle, not only Tk's internal client area.
- The app reapplies the title bar styling shortly after each window is created, covering the main dashboard and app dialogs.

## Verification

- `python -m py_compile patreon_member_gui.py export_patreon_members.py patreon_api.py`
- `python -m unittest discover -s tests`
- Local Tk smoke test confirmed that DWM dark mode is applied to the decorated frame handle.
