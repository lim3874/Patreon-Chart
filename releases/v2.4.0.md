# v2.4.0 - Loading Feedback

## Added

- Added animated loading text to the `Gmail에서 불러오기` button while Gmail import is running.
- Added matching loading feedback to the Patreon current-member import button.

## Changed

- Import buttons now return to their normal labels and enabled state through a shared loading-state handler after background work finishes.

## Verification

- `python -m py_compile patreon_member_gui.py export_patreon_members.py patreon_api.py`
- `python -m unittest discover -s tests`
- Local Tk smoke test for Gmail and Patreon loading start/stop button states.
