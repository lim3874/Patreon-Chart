# Patreon Chart v1.1.1

Bug fix release.

## Fixed

- Dark-mode text contrast for standard buttons.
- Dark-mode text contrast for readonly comboboxes.
- Dark-mode text contrast for preset date fields.
- Preset date fields now use `readonly` instead of `disabled`, so their values remain readable.

## Verification

```powershell
python -m py_compile .\patreon_member_gui.py .\patreon_api.py .\export_patreon_members.py
python -m unittest discover -s tests
```
