# Patreon Chart v1.0.1

Bug fix release.

## Fixed

- The GUI now saves the dark mode toggle to `app_settings.json`.
- Restarting the app restores the previously selected light/dark theme.

## Security

- `app_settings.json` is ignored because it is a local runtime settings file.

## Verification

```powershell
python -m py_compile .\patreon_member_gui.py .\patreon_api.py .\export_patreon_members.py
python -m unittest discover -s tests
```
