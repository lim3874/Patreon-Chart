# v2.6.0 - Focused Period Counts

## Changed

- The `재구독` period chart now shows only rejoined-member counts.
- Multi-series period charts now draw categories as separate bars rather than stacked total bars.
- The metric cards under period charts continue to show only the visible category totals for the selected chart mode.

## Verification

- `python -m py_compile patreon_member_gui.py export_patreon_members.py patreon_api.py`
- `python -m unittest discover -s tests`
- Local Tk smoke test for rejoin-only period buckets and grouped period chart rendering.
