# v2.13.6 - Fix Minimized Title Strip

## Fixed

- Removed the default Windows title strip that could appear near the bottom-left of the screen when minimizing the app.
- The custom borderless window now minimizes through the native Win32 `ShowWindow(SW_MINIMIZE)` path instead of toggling the Tk default frame.

## Verification

- `python -m py_compile patreon_member_gui.py export_patreon_members.py patreon_api.py discord_api.py`
- Tk smoke test for the Win32 custom minimize fallback path.
