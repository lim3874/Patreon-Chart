# v2.6.2 - Monthly Label Spacing

## Fixed

- Sparse monthly period charts now reserve wider slots for `YYYY-MM` labels.
- Two-month charts keep the bars centered without letting the month labels overlap.

## Verification

- `python -m py_compile patreon_member_gui.py export_patreon_members.py patreon_api.py`
- `python -m unittest discover -s tests`
- Local Tk smoke test confirming `2026-04` and `2026-05` labels do not overlap.
