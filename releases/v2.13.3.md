# v2.13.3 - Browser-Like Column Drag Smoothness

## Fixed

- Reused the Patreon API column drag preview widget between drags instead of destroying and recreating it.
- Cached preview sizes per column label to avoid repeated layout measurement.
- Coalesced drag movement into Tk idle frames so the preview tracks the latest mouse position with less repaint pressure.
- Lowered the drag start threshold slightly for a faster, browser-tab-like response.

## Verification

- `python -m py_compile patreon_member_gui.py export_patreon_members.py patreon_api.py discord_api.py`
- Tk smoke test for creating, centering, moving, hiding, and reusing the Patreon table drag preview.
